# Archived components (Feb 2025)

Unused legacy components. Daily challenges now live only on the Today tab; these are kept for reference.

- **OnboardingMissionModal.jsx** – Old onboarding modal with first-challenge flow. No longer rendered; first challenge is inline on Today.
- **ChallengeQueue.jsx** – Old modal queue for challenges. No longer rendered; challenge flow is inline on Today.

Do not import these in new code.
