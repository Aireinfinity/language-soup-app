-- Debug: Why isn't the function processing challenges automatically?

-- Check what the function is actually doing
SELECT auto_send_approved_challenges();

-- This will show us if there's an error or if it's working
